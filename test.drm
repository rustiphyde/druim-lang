global_value = 1;

:{
    block_value = 2;
    loc first_only = 3;

    |> (global_value);
    |> (block_value);
    |> (first_only);
}{
    second_value = 4;
    glo forced_global = 6;

    |> (global_value);
    |> (block_value);
    |> (second_value);
    |> (forced_global);
}: